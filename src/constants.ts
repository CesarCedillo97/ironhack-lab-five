export enum PageStatus {
  IDLE = "IDLE",
  LOADING = "LOADING",
  ERROR = "ERROR",
  NO_CONTENT = "NO_CONTENT",
}

export const UsersURL = "https://jsonplaceholder.typicode.com/users";

export const MaxRetrys = 5;
